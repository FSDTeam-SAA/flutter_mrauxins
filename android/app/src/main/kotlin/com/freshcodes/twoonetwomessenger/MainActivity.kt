package com.freshcodes.twoonetwomessenger

import io.flutter.embedding.android.FlutterActivity

// Screenshot protection (FLAG_SECURE) is toggled per-chat from Dart via the
// screen_protector plugin — do not set FLAG_SECURE unconditionally here.
class MainActivity : FlutterActivity()
